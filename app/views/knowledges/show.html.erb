<h3>投稿詳細</h3>
<div class="knowledge">
  <p><%= @knowledge.about %></p>
  <p><%= @knowledge.created_at %></p>
</div>

<%= link_to "投稿一覧に戻る", knowledges_path %>
<%= link_to "編集する", edit_knowledge_path(@knowledge.id) %>